[build]
builder = "NIXPACKS"
buildCommand = "cd frontend && rm -rf dist && npm install && npm run build && cd ../backend && npm install"

[deploy]
startCommand = "cd backend && npm run db:migrate && npm run db:seed:users && npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[services]]
name = "fwd-sales"
